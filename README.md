# RISE Client (WITHU 프론트엔드)

WITHU — AI 기반 개인 맞춤 건강 미션과 그룹 동기부여를 결합한 웰니스 그룹 서비스의 프론트엔드입니다.
멋쟁이사자처럼 대학 14기 중앙 해커톤(ANIMAL LEAGUE) **AAC 트랙** 출품작, 성결대 3팀.

## 이 문서 읽는 법

**화면만 손보실 거면 1~4번이면 충분합니다.** 5번은 서버와 얽힌 부분을 고칠 때
반드시 읽어야 하는 목록입니다 — 여기 적힌 것들은 전부 한 번씩 실제로 터졌던 것입니다.

1. [배포된 주소](#배포된-주소)
2. [기술 스택](#기술-스택)
3. [시작하기](#시작하기)
4. [구조](#구조)
5. [고칠 때 꼭 알아야 할 것](#고칠-때-꼭-알아야-할-것)
6. [작업 기록](#작업-기록)
7. [남은 것](#남은-것)
8. [브랜치](#브랜치)

---

## 배포된 주소

| 무엇 | 주소 |
|---|---|
| **웹앱** | https://rise-client-rohdaeyoungs-projects.vercel.app |
| 백엔드 API 문서 | https://1-201-117-9.nip.io/swagger-ui.html |
| 백엔드 | 같은 저장소의 [`backend/`](../backend) (제출 저장소는 https://github.com/rohdaeyoung/RISE) |

**테스트 계정: `test@withu.app` / `withu1234`** (그룹 코드 `TEAM33`, Day 7 상태)

`main`에 push하면 Vercel이 자동으로 다시 배포합니다.

> **2026-08-18: 백엔드가 Railway에서 해커톤 제공 서버(가비아 클라우드)로 옮겨졌습니다.**
> 주소가 IP처럼 생긴 건 도메인을 사지 않고 HTTPS를 붙이려고 `nip.io`를 쓴 것입니다
> (`1-201-117-9.nip.io` → `1.201.117.9`). 프론트가 https라 백엔드도 https여야 하는데,
> 브라우저가 https 페이지에서 http 요청을 막기 때문입니다.
>
> `.env.local`을 예전 Railway 주소로 두고 있다면 위 "실제 백엔드에 붙여서 개발하기"의
> 명령으로 다시 만들어 주세요. Railway는 만일을 대비해 **8/25까지만** 켜둡니다.

---

## 기술 스택

Vite · React 19 · React Router (HashRouter) · Tailwind CSS v4 · lucide-react

## 시작하기

```bash
cd frontend
npm install
npm run dev
```

`http://localhost:5173` 이 열립니다. **이대로도 그냥 돌아갑니다** — 백엔드 없이
브라우저 안에서만 도는 mock 모드입니다. 화면 작업만 할 거라면 이걸로 충분합니다.

### 실제 백엔드에 붙여서 개발하기

```bash
cd frontend
echo "VITE_API_BASE_URL=https://1-201-117-9.nip.io" > .env.local
npm run dev
```

배포된 백엔드에 그대로 붙습니다. 별도 서버를 띄울 필요가 없습니다.
`.env.local`은 git에 올라가지 않으니 각자 만들어 쓰세요.

> **주의**: 배포된 백엔드는 실제 DB를 씁니다. 여기서 만든 계정과 그룹은 다른 팀원에게도 보입니다.
> 마음껏 부수면서 실험하려면 백엔드를 로컬로 띄우세요 (`RISE-server` README의 "빠르게 실행하기").

`VITE_API_BASE_URL`을 지우면 다시 mock 모드가 됩니다. 백엔드 없이도 데모할 수 있도록
일부러 분리해 둔 구조이니, **연동한다고 mock 코드를 지우지 마세요.**

### 어느 주소에서 백엔드를 부를 수 있는지 (2026-08-15부터)

전에는 백엔드가 아무 사이트의 요청이나 받아줬는데, 보안을 위해 **허용된 주소에서만** 부를 수
있도록 좁혔습니다(백엔드의 `CORS_ALLOWED_ORIGINS`). 아래 셋은 지금 다 등록돼 있어서 평소처럼
개발하시면 됩니다.

| 주소 | 상태 |
|---|---|
| `https://rise-client-rohdaeyoungs-projects.vercel.app` (실서비스) | 허용 |
| Vercel 미리보기 (`https://rise-client-<해시>-rohdaeyoungs-projects.vercel.app`) | 허용 |
| `http://localhost:5173` (로컬 개발) | 허용 |

**여기 없는 주소에서는 API 호출이 전부 막힙니다.** 브라우저 콘솔에 `CORS` 오류가 뜨고 화면이
비어 보이면 이걸 의심하세요. 예를 들어 이런 경우입니다.

- `npm run dev -- --port 3000`처럼 **다른 포트**로 띄웠을 때
- 다른 Vercel 계정이나 새 프로젝트로 배포했을 때

프론트를 고쳐서 될 일이 아니라 **백엔드 환경변수에 그 주소를 추가**해야 합니다. 대영님께
말씀해 주세요 (`RISE-server` README의 "CORS 설정값").

---

## 구조

```
frontend/src
  ├── api/          서버 통신. mock ↔ 실제 백엔드 전환도 여기서 일어남
  │   ├── client.js       ★ 공통 fetch 래퍼, 토큰 처리, 이미지 주소 변환
  │   ├── authApi.js      로그인 / 회원가입
  │   ├── groupApi.js     그룹 생성·참여·설정·나가기
  │   ├── missionApi.js   미션 조회·인증, 시간대별 잠금 해제
  │   ├── mealApi.js      식단 사진 업로드 / AI 분석
  │   ├── profileApi.js   내 정보, 캐릭터, 온보딩, 상점, 랭킹
  │   ├── challengeApi.js 7일 챌린지 종료 / 결과 / 계속하기
  │   └── feedApi.js      그룹 피드 반응·댓글
  ├── context/
  │   └── AppContext.jsx  ★ 앱 전체 상태(useReducer) + 서버 동기화
  ├── pages/        화면 단위 컴포넌트
  ├── components/   재사용 UI (캐릭터, 미션 카드, 결과 시트 등)
  └── utils/        이미지 리사이즈 등
```

★ 표시한 두 파일이 핵심입니다. 고치기 전에 먼저 읽으세요.

### `client.js` — 모든 통신이 지나가는 곳

- `isBackendEnabled` : `VITE_API_BASE_URL` 설정 여부. 각 API 파일이 이 값으로 mock/실서버를 가름
- `api.get/post/patch/delete/postForm` : 서버 응답 `{ success, data, error }` 껍데기를 벗겨 `data`만 돌려줌
- `fileUrl(path)` : 서버가 주는 `/api/files/{id}` 같은 상대 경로를 완전한 주소로 바꿈.
  **이걸 안 거치면 이미지가 프론트 서버를 찾아가서 깨집니다.**

### `AppContext.jsx` — 상태와 동기화

- `useReducer` 기반. 화면은 `useAppState()` / `useAppDispatch()`로 접근
- `sync()` 가 **15초마다** 서버에서 내 정보·캐릭터·그룹·미션·식단·오늘의 피드를 다시 받아옴
- 상태는 localStorage에 자동 저장되어 새로고침해도 남음

---

## 고칠 때 꼭 알아야 할 것

### 1. 서버에 남아야 하는 값은 반드시 API를 호출하세요

가장 많이 낸 실수입니다. `dispatch`만 하면 **내 화면에서만 잠깐 바뀌었다가,
15초 뒤 `sync()`가 서버 값을 도로 가져와 원래대로 돌아갑니다.**

```js
// ✗ 이렇게 하면 그룹을 나간 것처럼 보였다가 다시 들어와집니다
function handleLeaveGroup() {
  dispatch({ type: 'LEAVE_GROUP' });
  navigate('/my');
}

// ✓ 서버에도 알려야 진짜로 나가지고, 실패했을 땐 화면을 바꾸지 않아야
//   "나갔는데 다시 들어와 있다"처럼 보이지 않습니다
function handleLeaveGroup() {
  leaveGroup()
    .then(() => {
      dispatch({ type: 'LEAVE_GROUP' });
      navigate('/my');
    })
    .catch((e) => setError(e?.message || '나가기에 실패했어요'));
}
```

실제로 **그룹 나가기, 방 이름 변경, 미션 시작 시간 변경** 세 가지가 이 문제였습니다.
상태를 바꾸는 화면을 만들면 `dispatch` 옆에 API 호출이 있는지 확인하세요.

### 2. 서버가 "없다"고 하면 로컬도 지워야 합니다

`sync()`가 그룹 조회에 실패했을 때 **아무것도 안 하고 넘어가면**, 서버에서는 이미 나갔는데
화면에는 그룹이 계속 보이는 유령 상태가 됩니다. 그 안의 기능은 전부 403이 나면서
아무 반응이 없는 것처럼 보입니다.

단, **403(서버의 확답)과 네트워크 오류를 구분해야 합니다.** 잠깐 끊겼다고 그룹을 지우면 안 됩니다.
`client.js`가 던지는 오류에 `status`와 `code`를 실어 보내니 그걸로 판단하세요.

### 3. 반대로, 서버에 있는 값은 `sync()`가 받아와야 합니다

식단 인증이 **새로고침하면 사라지는** 문제가 있었습니다. `sync()`가 미션·그룹·코인은
받아오는데 식단만 빠져 있었기 때문입니다. 새 데이터를 서버에 저장하기 시작했다면
`AppContext.jsx`의 `sync()`에도 조회를 추가하세요.

### 4. 미션은 서버가 만듭니다 — 로컬에서 만들지 마세요

`generateDailyMissions()`는 **백엔드 없이 데모할 때만** 쓰는 mock 생성기입니다.
백엔드 모드에서 이걸 호출해 화면에 넣으면, 잠깐 보였다가 `sync()`가 서버 세트로 갈아끼우면서
**미션이 저절로 바뀐 것처럼 보입니다.** 그 사이 다른 그룹원에게는 서버 세트가 보이므로
같은 사람의 오늘 미션이 서로 다르게 표시됩니다.

리듀서에서는 `newMissionSet()`을 쓰세요. 백엔드 모드면 빈 배열을 주고 서버가 채우게 합니다.
미션이 바뀌는 시점은 **날짜가 바뀔 때뿐**이어야 합니다.

### 5. 그룹 설정은 서버 값으로 계속 맞춰야 합니다

방 이름·미션 시작 시간처럼 **그룹원 전체가 공유하는 값**은 참여할 때 한 번 받고 끝내면 안 됩니다.

방을 만들 때는 초대 코드를 먼저 보여주려고 **이름 없이 방을 만든 뒤**(서버가 `건강한 친구들`로
지정) 나중에 이름을 정합니다. 그래서 그 사이에 참여한 사람은 방장이 지은 이름을 영영 못 봤습니다.
나중에 누가 방 이름이나 미션 시간을 바꿔도 같은 문제가 생깁니다.

`sync()`가 받아온 그룹 값을 `SET_GROUP_MEMBERS`에서 이름·미션 시간까지 함께 반영합니다.

### 6. 인증은 사진을 반드시 서버로 보내야 합니다

생활습관 미션 인증이 **사진 없이 호출되고 있었습니다.** 화면에는 "AI가 확인해요"라고 떠 있는데
정작 사진은 서버로 가지 않아, 걷기 미션에 채소 사진을 올려도 통과했습니다.

`completeMission(missionId, photo)`는 사진을 multipart로 보냅니다. 서버가 AI로 판정해
맞지 않으면 `MISSION_004`로 거절하므로, **거절됐을 때 완료로 표시하면 안 됩니다.**
`.catch`로 받아 메시지를 화면에 보여주세요.

### 7. 사이클이 끝나면 온보딩부터 다시입니다

"계속하기"는 같은 그룹으로 **새 7일 사이클**을 시작하는 것입니다. 서버가 지난 사이클의
미션·식단·온보딩을 지우므로, 곧바로 그룹 화면으로 보내면 온보딩이 없어 미션이 만들어지지 않습니다.
**온보딩 화면으로 보내야 합니다.**

목표·신체정보는 PRD상 사이클마다 다시 받는 값입니다. 7일 사이에 몸이나 목표가 달라졌을 수 있고,
그래야 AI가 새 기준으로 미션을 만듭니다.

### 8. 미션 개수는 3개가 아닐 수 있습니다

백엔드가 상황에 따라 **1개 / 3개 / 4개**를 내려줍니다
(달성률 90% 이상이면 4개, 3일 연속 실패하면 1개).
3개를 전제로 짜면 화면이 깨집니다.

### 9. 이미지 주소는 `fileUrl()`을 거치세요

서버는 `/api/files/{uuid}` 형태의 상대 경로를 줍니다. 그대로 `<img src>`에 넣으면
프론트 주소를 찾아가서 깨집니다.

### 10. `internalFit`은 화면에 띄우지 마세요

식단 분석 결과의 목표적합도(`good`/`normal`/`bad`)는 **다음 미션 난이도를 정하는 내부 값**입니다.
기획상 사용자에게는 "달성 / 미달성"만 보여줍니다. (애초에 서버가 응답에 담지 않습니다.)

### 11. 이미지가 무거우니 단일 파일 번들로 되돌리지 마세요

`vite-plugin-singlefile`을 쓰면 캐릭터·의상 이미지까지 HTML 하나에 인라인되어
첫 화면이 14MB가 되고, 다 받을 때까지 흰 화면이 유지됩니다.
나눠서 내보내면 처음엔 100KB 정도만 받습니다. 자세한 건 `vite.config.js` 주석 참고.

### 12. 인증 화면에는 "무엇을 찍어야 하는지"가 보여야 합니다

식단 인증(`MealUploadPage`)은 끼니 이름만 알고 미션 제목은 모릅니다. 그래서 예전에는 어떤 미션이든
"아침 사진 업로드"만 떴고, 사용자는 무엇을 찍어야 하는지 알 수 없었습니다.

지금은 `state.missions`에서 **아직 완료되지 않은 첫 번째 식단 미션**을 찾아 제목을 보여줍니다.
서버도 같은 규칙으로 판정 기준을 고르므로(`MissionService.pendingDietMissionTitle`),
이 규칙을 바꾸면 **화면에 보여준 미션과 실제로 판정되는 미션이 어긋납니다.**

### 13. 미션은 하루치가 한 번에 열립니다

예전에는 +3.5h/+7h로 나눠 열려서 "다음 미션은 오후 12:30에 도착해요"가 떴습니다.
PRD 8번은 "설정된 시간에 미션이 동시에 생성된다"이므로, 지금은 세트 전체가 함께 열립니다.
그룹의 미션 시각은 **세트가 도착하는 시각**이지, 미션이 하나씩 열리는 간격이 아닙니다.

`nextUpcomingMission`·`visibleMissions`·`isMissionUnlocked`는 남겨뒀습니다.
서버가 다시 `unlockTime`을 채워 보내면 그대로 동작합니다.

### 14. 탈퇴·나가기는 서버 응답을 기다린 뒤에 화면을 바꾸세요

탈퇴 버튼이 `logout()` + `dispatch`만 하고 있었습니다. 화면에서만 사라지고 DB에는 계정이
그대로 남아, 같은 이메일로 재가입이 막혔습니다.

`deleteAccount()`는 `DELETE /api/auth/me`를 부릅니다. 두 가지를 지켜야 합니다.

- **토큰은 서버 삭제가 끝난 뒤에** 지웁니다. 먼저 지우면 삭제 요청이 401로 거절됩니다.
- **실패하면 화면을 바꾸지 않습니다.** 실패했는데 로그아웃시키면 지워진 줄 알게 됩니다.

같은 원칙을 그룹 나가기·방 설정 저장·캐릭터 생성에도 적용했습니다 (아래 "최근 수정 (2026-08-15)" 참고).

### 15. 401(로그인 만료)과 403(그룹원 아님)은 다릅니다

`client.js`는 **401 + `AUTH_003`**만 로그인 만료로 봅니다. 이때 토큰을 지우고
`AppContext`가 로그인 화면으로 되돌립니다.

두 가지를 섞지 마세요.

- **403은 만료가 아닙니다.** "로그인은 됐지만 그 그룹의 그룹원이 아니다"(`GROUP_005`)라는
  뜻으로 이미 쓰고 있습니다. 403까지 만료로 처리하면 그룹만 나간 사람이 로그아웃됩니다.
- **401을 전부 만료로 보면 안 됩니다.** 로그인 실패(`AUTH_002`)도 401이라,
  비밀번호를 한 번 틀렸을 때 "로그인이 만료됐어요"가 뜹니다. 그래서 코드까지 확인합니다.

이 처리가 없을 때 무슨 일이 있었는지는 아래 "최근 수정 (2026-08-18)"에 적어뒀습니다.

### 16. 미션이 밋밋하면 프론트 문제가 아닙니다

미션 문구가 매일 비슷하거나 사진 인증이 `잠시 후 다시 시도해주세요`로 계속 실패하면,
백엔드의 OpenAI 사용량 한도(무료 등급 하루 50회)가 찬 것입니다. 한도를 넘기면 서버가
고정 문구로 대체하기 때문에 **화면은 멀쩡한데 내용만 밋밋해집니다.**

프론트에서 고칠 것은 없습니다. 백엔드 담당자에게 알려주세요.
자세한 내용은 `RISE-server/README.md`의 "OpenAI 사용량 한도" 항목에 있습니다.

---

## 작업 기록

지난 수정 내역입니다. 같은 실수를 반복하지 않으려고 남겨 둡니다.

### 2026-08-14

팀 피드백으로 올라온 문제들을 반영했습니다. 관련 파일과 함께 정리합니다.

1. **미션이 시간대별로 나눠 오던 문제** — "다음 미션은 O시에 도착해요"가 뜨던 것을,
   하루치(식단 2개 + 생활습관 1개)가 한 번에 도착하도록 변경. (`api/missionApi.js`)
2. **걷기 미션에 아무 사진이나 인증되던 문제** — 생활습관 미션 인증이 사진 없이 호출되고
   있어서, 걷기 미션에 음식 사진을 올려도 통과했습니다. 사진을 실제로 서버에 함께
   전송하도록 수정. (`api/missionApi.js`, `pages/MissionVerifyPage.jsx`)
3. **식단 인증 화면에 미션 제목이 안 뜨던 문제** — 모든 끼니에서 "아침 사진 업로드" 같은
   똑같은 문구만 떴던 것을, 실제 미션 제목을 보여주도록 변경. (`pages/MealUploadPage.jsx`)
4. **7일 결산에서 구매한 옷이 반영 안 되던 문제** — 랭킹·그룹 피드에는 넘기던 `outfit`
   값을 결산 화면(`ChallengeSummarySheet`)에는 빼먹어서, 상점에서 산 옷을 입고 있어도
   결산에서만 기본 옷으로 보였습니다. (`components/ChallengeSummarySheet.jsx`)
5. **계정 탈퇴가 로컬에서만 처리되던 문제** — 로그아웃만 하고 화면에서 사라진 것처럼
   보였지만 서버 DB에는 계정이 그대로 남아, 같은 이메일로 재가입이 막혔습니다.
   서버 삭제 API를 실제로 호출하도록 수정. (`api/authApi.js`, `pages/SettingsPage.jsx`)
6. **그룹 나가기 이후에도 서버 상태를 못 따라가던 문제** — 그룹 조회가 실패했을 때
   네트워크 오류와 "진짜로 그룹원이 아님(403)"을 구분하지 않아, 나간 뒤에도 화면에
   그룹이 계속 보이는 유령 상태가 됐습니다. 에러에 상태 코드를 실어 구분하도록 수정.
   (`api/client.js`, `context/AppContext.jsx`)
7. **미션이 저절로 바뀌어 보이던 문제** — 그룹/온보딩 시점에 프론트가 임시로 만든
   미션이 화면에 잠깐 보였다가, 서버 동기화가 실제 미션 세트로 갈아끼우면서 미션이
   저절로 바뀐 것처럼 보였습니다. 백엔드 연동 모드에서는 프론트가 미션을 만들지
   않도록 정리. (`context/AppContext.jsx`)
8. **방 이름·미션 시간이 참여자에게 다르게 보이던 문제** — 그룹 설정 변경이 동기화에
   반영되지 않아, 나중에 참여한 사람이나 방장이 이름을 바꾼 뒤에도 예전 값이 그대로
   보였습니다. (`context/AppContext.jsx`)
9. **"계속하기" 시 온보딩 없이 새 사이클이 시작되던 문제** — 목표/신체정보를 다시
   묻지 않고 바로 그룹 화면으로 넘어갔던 것을, PRD대로 사이클마다 온보딩을 다시
   거치도록 수정. (`context/AppContext.jsx`, `components/ChallengeSummarySheet.jsx`)

---

### 2026-08-15

백엔드가 만들어둔 그룹 피드 API를 연결하고, 서버 거절이 조용히 묻히던 곳들을 정리했습니다.
아래 두 가지가 직전까지 "프론트에서 해야 할 일" 1순위·2순위였던 항목입니다.

1. **그룹 피드 반응·댓글이 내 기기에만 보이던 문제** — 반응(❤️👍😂😮😢)·댓글이
   `AppContext`에만 쌓여서 남긴 사람 화면에만 보이고 그룹원에게는 안 보였습니다.
   백엔드의 `GET/POST /api/feed` 세 엔드포인트에 연결해, 응답으로 온 갱신된 피드 전체를
   그대로 반영하도록 수정. 화면은 기존처럼 즉시 로컬 리듀서로 반영(낙관적 업데이트)하고,
   서버 응답으로 다시 한번 맞춰 그 사이 다른 그룹원이 남긴 것까지 함께 받아옵니다.
   (`api/feedApi.js` 신규, `context/AppContext.jsx`, `pages/GroupFeedPage.jsx`,
   `components/CommentSheet.jsx`)
2. **서버가 거절해도 조용히 묻히던 8곳** — 화면을 먼저 바꾼 뒤 서버에 보내고 `.catch`가
   없어서, 실패해도 아무 일 없다는 듯 넘어갔다가 15초 뒤 `sync()`가 조용히 되돌리고
   있었습니다. 성공했을 때만 화면을 바꾸고, 실패하면 사유를 보여주도록 수정.
   - `pages/ShopPage.jsx` — 의상 구매·착용, 캐릭터 종 변경
   - `pages/GroupCreatePage.jsx` — 그룹 생성 (실패 시 "다시 시도" 버튼 추가)
   - `pages/OnboardingPage.jsx` — 온보딩 저장 (실패해도 그룹 화면으로 넘어가던 것 수정)
   - `pages/SettingsPage.jsx` — 닉네임 저장
   - `pages/GroupSettingsPage.jsx` — 방 설정 저장, 그룹 나가기
   - `pages/CharacterCreatePage.jsx` — 캐릭터 생성, 닉네임 저장

3. **내 카드에만 반응이 남지 않던 문제** (1번을 연결하면서 생긴 것)
   그룹원 카드의 식별자를 그대로 `targetUserId`로 보냈는데, 화면은 **나를 `'me'`로
   부릅니다**(`buildRanking`). `Number('me')`는 `NaN`이고 JSON에서 `null`로 나가
   서버가 400으로 거절합니다. 받는 쪽도 서버 키는 실제 userId라 `reactions['me']`로는
   못 찾습니다. 결과적으로 내 카드에서만 반응이 안 남고 안 보였습니다.
   서버가 프론트 내부 표기를 알아야 하는 구조는 잘못이므로 **API는 그대로 두고 프론트에서
   변환**했습니다. (`pages/GroupFeedPage.jsx`, `context/AppContext.jsx`)

   ```js
   // 보낼 때 (GroupFeedPage.handleReact)
   const targetUserId = memberId === 'me' ? state.auth.userId : memberId;

   // 받을 때 (AppContext의 SET_FEED — state.auth.userId를 아는 유일한 곳)
   reactions[userId === myKey ? 'me' : userId] = value;
   ```

   **피드 코드를 고칠 때 이 변환을 깨지 마세요.** 깨지면 내 카드만 조용히 동작을 멈춥니다.
   반응이 거절되면 낙관적으로 바꿔둔 화면도 되돌립니다.

   지금은 반응 버튼이 남의 카드에만 뜨지만(`member.photo && !member.isMe`), **받는 쪽 변환은
   여전히 필수**입니다. 남이 내 카드에 남긴 반응은 서버에서 내 실제 userId 키로 내려오는데,
   내 화면은 그 카드를 `'me'`로 찾기 때문입니다.

4. **"동작 줄이기"를 켜면 하단 탭이 통째로 먹통이던 문제** — 탭 전환은 크로스페이드가
   끝나는 `onAnimationEnd`에서 화면을 교체하는데, 애니메이션이 아예 재생되지 않으면 그
   이벤트도 오지 않습니다. 주소만 `#/my`로 바뀌고 화면은 이전 탭에 멈춰 있었습니다.
   재생되지 않는 경우가 실제로 있습니다.
   - 사용자가 iOS·Android의 **"동작 줄이기"**를 켠 경우
     (`index.css`에서 `prefers-reduced-motion: reduce` → `animation: none`)
   - 브라우저가 백그라운드 탭의 애니메이션을 멈춘 경우

   애니메이션은 연출로만 두고, **화면 교체 자체는 시간이 지나면 반드시 일어나도록** 타이머를
   함께 걸었습니다. (`App.jsx`)

   > 전환 시간을 바꿀 땐 `App.jsx`의 `EXIT_ANIMATION_MS`와 `index.css`의
   > `.page-transition-exit` 재생 시간을 같이 맞춰주세요.

5. **로그인 없이 주소를 직접 쳐서 `/my` 등 내부 화면에 들어가지던 문제** — 로그인 여부를
   확인하는 라우트 가드가 없어서, 인증 정보가 전혀 없는 상태로 `#/my` 같은 주소를 직접
   입력하면 로그인 화면을 거치지 않고 빈 상태(캐릭터 없음, 0%)의 화면이 그대로 떴습니다.
   서버 API는 토큰이 없으면 403으로 막아주지만 화면 자체는 노출되는 상태였습니다.
   `"/"`, `"/signup"`을 제외한 모든 라우트를 `RequireAuth`로 감싸서, 로그인 정보가 없으면
   로그인 화면으로 리다이렉트하도록 수정. (`App.jsx`)

---

## 남은 것

### 정리해도 되는 것

- `nextUpcomingMission` / `visibleMissions` / `isMissionUnlocked` — 미션이 하루치 한 번에
  열리도록 바뀌면서 지금은 항상 통과합니다. 서버가 다시 `unlockTime`을 채우면 그대로 동작하므로
  **남겨둬도 됩니다.** 지울 거면 `MyPage`의 "다음 미션은 ~에 도착해요" 블록도 같이 지우세요.
- `generateDailyMissions`의 `missionHour`/`missionMinute` 인자 — 이제 안 쓰지만 mock 모드
  호환을 위해 시그니처만 남아 있습니다.

### 백엔드에 알려야 하는 것

- **의상을 추가·교체하면 반드시 알려주세요.** `ShopPage.jsx`의 `OUTFIT_SETS`와 백엔드
  `OutfitCatalog.ITEMS`는 **id와 가격이 항상 같아야 합니다.** 한 번 어긋나서 세일러·코트·탐정을
  사려 하면 `SHOP_001 존재하지 않는 의상`으로 거절됐고, 파자마는 프론트 30 / 백엔드 35코인이었습니다.
  (지금은 맞춰져 있습니다: pajama 30 / sailor 35 / coat 40 / detective 50.)
- **미션이 밋밋하거나 사진 인증이 계속 실패하면** 프론트 문제가 아닙니다. 백엔드의 OpenAI
  사용량 한도(무료 등급 하루 50회)가 찬 것입니다. 한도를 넘기면 서버가 고정 문구로 대체해서
  화면은 멀쩡한데 내용만 밋밋해집니다. 자세한 건 `RISE-server/README.md`의 "OpenAI 사용량 한도".

## 브랜치

`main`이 기준입니다. Vercel이 여기서 자동 배포합니다.

작업할 때는 `feature/FE-*` 브랜치를 파고, 끝나면 `main`으로 합쳐주세요.

```bash
git checkout main
git pull
git checkout -b feature/FE-작업이름
```
